(executable
    (name main)
    (public_name gomoku)
    (libraries graphics)
    (modes native)
)

(env
    (dev
        (flags (:standard -warn-error -A -w +A))
    )
)

(include_subdirs unqualified)
